apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pmem-csi-pvc-ephemeral
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: pmem-csi-sc-ephemeral # defined in pmem-storageclass-ephemeral.yaml
